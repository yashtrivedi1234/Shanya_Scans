name: shanya_scans
description: "Shanya"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.6+15

environment:
#  sdk: ^3.6.0
   sdk: '>=3.6.0 <4.0.0'


# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  carousel_slider: ^5.0.0
  url_launcher: ^6.1.11
  razorpay_flutter: ^1.4.0
#  flutter_html: any

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  device_preview: ^1.2.0
  provider: ^6.1.2
  dio: ^5.8.0+1
  shared_preferences: ^2.5.1
  json_serializable: ^6.9.3
  json_annotation: ^4.9.0
  animated_text_kit: ^4.2.3
  otp_text_field: ^1.1.3
  shimmer: ^3.0.0
  flutter_html: ^3.0.0-beta.2
  share_plus: ^12.0.0
  webview_flutter: ^4.10.0
  connectivity_plus: ^7.0.0
  cached_network_image: ^3.4.1
  easy_stepper: ^0.8.5+1
  lottie: ^3.3.1
  package_info_plus: ^9.0.0
  flutter_svg: ^2.0.17
  socket_io_client: ^3.1.2
  easy_date_timeline: ^2.0.9
  geolocator: ^14.0.1
  geocoding: ^4.0.0
  google_maps_flutter: ^2.12.0  # For displaying the map
  flutter_polyline_points: ^3.0.1  # For drawing the route
  permission_handler: ^12.0.1
  google_maps_flutter_platform_interface: ^2.2.3
  firebase_core: ^4.1.0
  firebase_messaging: ^16.0.1
  google_places_flutter: ^2.0.5
  flutter_background_service: ^5.0.5
  app_tracking_transparency: ^2.0.4
  path_provider: ^2.0.11
  open_filex: ^4.0.2
  external_path: ^2.2.0
  device_info_plus: ^10.0.0
  upgrader: ^12.3.0


  intl: any
  fluttertoast: any
  html: any
  uuid: any
  http: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.3
  flutter_native_splash: ^2.4.6
  change_app_package_name: ^1.5.0
#  flutter_local_notifications: ^18.0.1

flutter_native_splash:
  color: "#ffffff" # White background
  image: "assets/images/transition_splash_logo.png" # Your SHANYA logo

  # New additions from your provided image:
  background_image: "assets/images/transition_splash_logo.png" # You'll need to create this image
#  branding: "assets/branding.png" # You'll need to create this image

  # Dark mode specific assets (optional, uncomment if you plan to use dark mode)
  # background_image_dark: "assets/background_dark.png"
  # image_dark: "assets/logo_dark.png"
  # branding_dark: "assets/branding_dark.png"

  # Android 12 specific configuration
  android_12:
    color: "#ffffff" # The specific color from your image
    image: "assets/images/transition_splash_logo.png" # You'll need to create this image for Android 12
#    branding: "assets/images/simple_logo.png" # You'll need to create this image for Android 12

  # Other existing settings from your previous configuration:
  android: true
  ios: true
  web : true
  fullscreen: true
  android_gravity: center

flutter_launcher_icons:
  android: true
  ios: true
#  web : true
  remove_alpha_ios: true
  image_path_android: "assets/images/simple_logo.png"
  image_path_ios: "assets/images/simple_logo.png"


  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
#  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/images/navicon/
    - assets/lottie/
    - assets/svg/
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Poppins
      fonts:
        - asset: assets/fonts/poppins_regular.ttf
#          weight: 400
#        - asset: assets/fonts/lato_regular.ttf
#          weight: 500
#        - asset: assets/fonts/lato_regular.ttf
#          weight: 800
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
